// import { Coordinate, UiLayout } from 'trainbrain-shared';
// import { trackLayoutDb } from '../services/db.js';

// // Set the track layout start position (and write it to the DB)
// export async function setStartPosition(x: number, y: number, heading: number): Promise<UiLayout> {
//   const startPosition: Coordinate = {x, y, heading}
//   trackLayoutDb.data.startPosition = startPosition;
//   await trackLayoutDb.write();

//   return getLayout();
// }
